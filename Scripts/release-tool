#!/usr/bin/env bash

set -euo pipefail

script_directory="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
repository_root="$(cd "${script_directory}/.." && pwd)"

# Homebrew Ruby is keg-only; /opt/homebrew/bin/ruby need not exist.
export PATH="/opt/homebrew/opt/ruby/bin:/opt/homebrew/bin:/opt/homebrew/sbin:${PATH}"
export BUNDLE_GEMFILE="${repository_root}/Gemfile"

command -v ruby >/dev/null 2>&1 || { echo "Ruby is required" >&2; exit 69; }
command -v bundle >/dev/null 2>&1 || { echo "Bundler is required" >&2; exit 69; }
exec bundle exec ruby "${repository_root}/fastlane/bin/sa-release" "$@"
